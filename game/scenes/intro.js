define([
        'underscore',
        'crafty',
        'game/config',
        'storage'
],
    function (_, Crafty, gameConfig, storage) {
        return {
            name: "intro",
            init: function (options) {

            },
            uninit: function () { }
        };
    });